# 参与改进

欢迎提交规则修正、测试样例和可复用模板。为保护项目资料并保持规则可信，请遵循以下要求。

## 提交前

1. 不提交真实市县级原始数据、样点坐标、未公开报告、超大DOCX、GIS成果或个人信息。
2. 正式规范规则须注明文件名、发布机构、版本、条款和页码；不要仅引用二手解读。
3. 个别市县样稿的做法只能标为样稿惯例，不能写成全国统一要求；地名带“市”不能替代行政层级判断。
4. 新增硬门槛必须说明适用范围、冲突处理和失败行为。
5. 代码改动须补充对应合成测试，不用真实市县事实作为测试数据。

## 本地验证

```powershell
python -m unittest discover -s scripts -p "test_*.py"
python scripts/chronicle.py check "<合成工作台>"
```

涉及DOCX排版时，还须在目标软件中重新打开、更新字段、全页渲染并记录回归结果。静态脚本通过不能替代视觉检查。

## 建议的变更说明

- 问题及影响范围
- 采用的依据和定位
- 修改前后行为
- 新增或更新的测试
- 仍不能自动判断的边界
